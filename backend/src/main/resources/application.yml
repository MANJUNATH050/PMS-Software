spring:
  datasource:
    url: ${DB_URL}
    driver-class-name: ${DB_DRIVER:org.postgresql.Driver}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}

  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
    properties:
      hibernate:
        format_sql: false
        dialect: ${DB_DIALECT:org.hibernate.dialect.PostgreSQLDialect}

server:
  address: 0.0.0.0
  port: ${PORT:8085}

app:
  jwt:
    secret: ${JWT_SECRET}
    expiration: ${JWT_EXPIRATION:86400000}
  frontend-url: ${PMS_FRONTEND_URL:http://localhost:5173}

brevo:
  api-key: ${BREVO_API_KEY}
  sender-email: ${BREVO_SENDER_EMAIL}
  sender-name: ${BREVO_SENDER_NAME:Aseuro PMS}




















# spring:
#     datasource:
#         url: ${DB_URL:jdbc:postgresql://localhost:5433/pms_db}
#         driver-class-name: ${DB_DRIVER:org.postgresql.Driver}
#         username: ${DB_USERNAME:postgres}
#         password: ${DB_PASSWORD:postgres}
#     jpa:
#         hibernate:
#             ddl-auto: update
#         open-in-view: false
#         properties:
#             hibernate:
#                 format_sql: false
#                 dialect: ${DB_DIALECT:org.hibernate.dialect.PostgreSQLDialect}
# # server:
# #     port: 8085/
# server:
#     address: 0.0.0.0
#     port: ${PORT:8085}
# app:
#     jwt:
#         secret: ${JWT_SECRET:K7mQ2xP9vL4nR8sT3wY6aB1cD5eF0gH2}
#         expiration: ${JWT_EXPIRATION:86400000}
#     frontend-url: ${PMS_FRONTEND_URL:${FRONTEND_URL:http://localhost:5173}}
# brevo:
#     api-key: ${BREVO_API_KEY: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}
#     sender-email: ${BREVO_SENDER_EMAIL:demomanjunath@gmail.com}
#     sender-name: ${BREVO_SENDER_NAME:Aseuro PMS}
